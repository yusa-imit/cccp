---
description: Codex channels peer
allowed-tools:
  - mcp__ccp-inbox__list_peers
---

Call the `list_peers` MCP tool from the `ccp-inbox` server and present the result as a short table (name, url, uptime). If empty, tell the user no other CCP instances are running and remind them to start another Codex session that has the ccp plugin enabled.
